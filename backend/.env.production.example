# Production Environment Variables for Vercel Deployment
# Copy these to Vercel Dashboard → Settings → Environment Variables

# Node Environment
NODE_ENV=production

# MongoDB Atlas Connection String
# Get this from MongoDB Atlas → Connect → Connect your application
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/messmate?retryWrites=true&w=majority

# JWT Secrets (Generate using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_ACCESS_SECRET=your-super-secret-access-key-min-32-chars-here
JWT_REFRESH_SECRET=your-super-secret-refresh-key-min-32-chars-here

# Frontend URL (Your Vercel frontend URL)
FRONTEND_URL=https://your-frontend.vercel.app

# CORS Origins (comma-separated list)
CORS_ORIGIN=https://your-frontend.vercel.app,http://localhost:5173

# Bcrypt Rounds
BCRYPT_ROUNDS=10

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# JWT Expiration
JWT_ACCESS_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d
